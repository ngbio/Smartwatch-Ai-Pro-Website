import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// TODO: Configure Vite build and React plugin options for the frontend app.
export default defineConfig({
  plugins: [react()],
});
